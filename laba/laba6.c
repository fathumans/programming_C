#include <stdio.h>
#include <math.h>
#define Max_Name_Length 100

int main()
{
    File
    
    char Familia[Max_Name_Length];
    char Imya[Max_Name_Length];
    char Otchevstvo[Max_Name_Length];
    int Year;
    
    while (fscanf(file, "%s %s %s %d", Familia, Imya, Otchevstvo , Year) == 4) {
        if year > 1980 {
            fprintf(outputfile, "%s %s %s %d", Familia, Imya, Otchevstvo, Year)
        }
    }



    return 0;
}
